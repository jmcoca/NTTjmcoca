spring:
  application:
    name: thymeleaf-dropzone
  cloud:
    config:
      uri: http://localhost:9090
      username: root
      password: 1234
  boot:
    admin:
      client:
        url: http://localhost:8086


spring.servlet.multipart.max-file-size:
  5MB
spring.servlet.multipart.max-request-size:
    5MB